# Environment variables required for connector_backend_api
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=connector_platform
# Optional encryption key for token fields (must be base64-url-safe encoded 32-byte key)
# You can generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=
